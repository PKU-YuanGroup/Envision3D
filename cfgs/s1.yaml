pretrained_model_name_or_path: ./pretrained_models/s1_pipe

revision: null
validation_dataset:
    root_dir: ./processed_imgs/
    bg_color : "white"
    crop_size : 224
    img_wh : [256, 256]
    filepaths: ['pumpkin.png']

seed: 43
validation_batch_size: 1

enable_xformers_memory_efficient_attention: true

dataloader_num_workers: 64

save_dir: './img_outputs/'